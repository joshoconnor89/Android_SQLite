apply plugin: 'com.android.application'

android {
    compileSdkVersion 16
    buildToolsVersion "21.1.0"

    defaultConfig {
        applicationId "com.example.sqliteexample2"
        minSdkVersion 8
        targetSdkVersion 15
    }

    buildTypes {
        release {minifyEnabled
        }
    }
}

dependencies {
    compile 'com.android.support:support-v4:18.0.0'
}
